# 6.1.1 — Account Health-Snapshot Rollup

**Plan ID:** 6.1.1 (Phase 6, first Post-Sale Growth Loop session)
**Depends on:** nothing blocking in the tracker. First Phase 6 build.
**File:** `6.1.1-Account-Health-Snapshot-Rollup.json`

## Why this session's spec came from §8, not a Sales Funnel stage doc

Stage 43 (Account Management), which this session maps to, is one of the stages flagged in this master plan's own §16.0 correction as **not yet built to pilot depth** — no `automation.md` exists for it. Per the master plan's read-across note (§16.0/§10B row 43) and the golden rule in the supporting context ("when the workflow can be built without the missing information, build the unblocked part"), this session's own node list (6.1.1.1–6.1.1.6, already in the tracker) is treated as the spec of record instead. Nothing here pretends to be sourced from Stage 43's `automation.md`, because that file doesn't exist yet.

## What's real vs. flagged in this build

| Component | Status |
|---|---|
| Cron cadence (weekly Mon 7AM) | Placeholder, matches existing 2.8/2.9 cadence patterns — not a confirmed requirement |
| Usage-data pull | **Real** — reuses Module 2.8's exact Metabase card/session pattern, same env vars |
| Tickets-data pull | **Mocked and disabled** — support/ticketing tool is explicitly undecided (§0.3); do not enable in production without first resolving that decision |
| Last-contact pull | **Real** — reads `communication_events` (built in 4.3.3) with fallback to `clients_master.last_reply_at` |
| Health-score formula | **Placeholder** — the component logic (recency, churn-risk status, dunning attempts, ticket volume) is transparent and documented in the code node, but the point deductions and the `<40` "low health" cutoff are not sourced from anywhere in the uploads. Flagged in the output row itself (`health_score_methodology_status`) and must be approved by Nivy before being used for any renewal- or contract-affecting decision. |
| Odoo write | Uses the confirmed `execute_kw` JSON-RPC pattern, but `x_account_health_score` is a placeholder field name (same caveat as 5.1.2) |

## Manual steps before production

1. Decide the support/ticketing tool (§0.3 open item) and replace the `MOCK_TICKETING_PROVIDER` node with a real HTTP call once decided.
2. Get Nivy sign-off on the health-score weighting and the low-health cutoff, or supply the real rubric if one exists that wasn't in this upload.
3. Confirm `x_account_health_score` against a live `fields_get` call on `crm.lead` (same step as flagged in 5.1.2).

## Test Checklist

1. Run manually against a test account with recent `communication_events` rows → confirm `days_since_last_contact` is small and score stays high.
2. Run against an account with `status = 'Churn Risk'` and no contact in 60+ days → confirm score drops and it lands in `Log Low-Health Account for Review`.
3. Confirm the mock ticketing node stays disabled and doesn't silently zero out scores (check `ticket_data_source` in the row).
4. Confirm `clients_master.health_score` and `health_score_snapshot_at` are both written.

---

**Next in Phase 6 (per §8 tracker):** 6.2.1 — Adoption checklist auto-create, no blockers listed.
