# 6.2.2 — Milestone-Missed Alert

**Plan ID:** 6.2.2 (Phase 6, session 3)
**Depends on:** 6.2.1 (Adoption checklist auto-create) — now built.
**File:** `6.2.2-Milestone-Missed-Alert.json`

## What's real vs. flagged

| Component | Status |
|---|---|
| Daily 8AM cadence | Placeholder, matches 2.9's daily-check pattern — not sourced |
| `project.task.date_deadline` vs `stage_id.is_closed` query | Real `execute_kw` pattern, but **6.2.1's checklist tasks don't set a deadline today**, so this workflow will only catch tasks that got a deadline some other way (manual entry by delivery team, or a future patch to 6.2.1 to add one). Flagged in the workflow notes, not silently assumed to work end-to-end with 6.2.1 alone. |
| `discuss.channel` model name | Same Odoo-version ambiguity already on record elsewhere in this plan (`mail.channel` on Odoo <17 vs `discuss.channel` on 17+, §10A.3) — confirm against the live instance before relying on it. |
| Internal alert recipient email | `delivery-team@nivynext.internal` is a placeholder address — replace with the real distribution list. |

This alert is internal-only (Discuss channel + internal email), not sent to the client — consistent with the plan's existing pattern of keeping client-facing communication separate from internal ops alerts.

## Recommended follow-up (not built here, flagging per golden rule)

If Nivy wants actual milestone tracking (not just any task with any deadline), 6.2.1 should be patched to set `date_deadline` on at least the key checklist items when it creates them — otherwise this workflow will mostly return empty results. This is a real gap between the two sessions worth a quick decision, not something to silently paper over.

## Test Checklist

1. Manually set a past `date_deadline` on one Odoo task in a test project → run this workflow → confirm it lands in the alert branch with the correct overdue-days count.
2. Confirm a client project with no overdue tasks routes to `No Missed Milestones - Skip`.
3. Confirm both the Discuss post and the internal email fire together, not just one.

---

**Next in Phase 6:** 6.3.1 — Support/ticketing wiring. Note: this session's `Linear ticket-create node` assumes Linear, but §0.3 leaves the support/helpdesk tool as an explicitly **undecided** open item (Chatwoot vs. others) — building 6.3.1 next means building against Linear specifically as named in the tracker row, while flagging that it may conflict with the still-open helpdesk decision. Want me to proceed with 6.3.1 as literally specified (Linear), or hold it pending that decision?
