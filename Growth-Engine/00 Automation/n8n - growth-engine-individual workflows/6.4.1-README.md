# 6.4.1 — Upsell/Cross-sell Trigger

**Plan ID:** 6.4.1 (Phase 6, session 5)
**Depends on:** 6.1.1 (Account health) — built. Unblocked.
**File:** `6.4.1-Upsell-Crosssell-Trigger.json`

## Flag — thresholds are placeholders, not Stage 47 content

§10B row 47 (Upsell Identification) marks that stage 🟢/✅ at the business-doc level, meaning a real `automation.md` with real thresholds is claimed to exist in the Sales Funnel knowledge base — but that specific file's content wasn't part of this upload, only its existence was referenced. Per the golden rule (never invent thresholds, but build the unblocked structural part), the rule-engine here uses transparent placeholder cutoffs (`health_score >= 70`, `tenure >= 90 days`, adoption checklist completed) shaped like a reasonable "healthy, established account" rule, clearly marked `rule_engine_thresholds_status: PLACEHOLDER`. **If you have access to Stage 47's real `automation.md` (in `00_Marketing.zip`, not uploaded here), send it over and I'll swap in the real numbers.**

## Guardrail carried over from the plan's own top blocker

Module 2.4 (Proposal Generation) is flagged as this plan's #1 launch-blocker for letting Ollama hallucinate pricing directly into client-facing output (§10A.5/§16.1). This workflow deliberately avoids repeating that pattern: the Ollama prompt is explicitly told not to generate pricing, and the output lands as an Odoo activity (a to-do for a human sales rep) — not anything sent to the client directly.

## New env var needed

`ODOO_UPSELL_ACTIVITY_TYPE_ID` — parallel to the existing `ODOO_LINKEDIN_ACTIVITY_TYPE_ID` / `ODOO_RENEWAL_ACTIVITY_TYPE_ID` pattern. Needs to be created as an activity type in Odoo and added to the env var list (§10A.2).

## Test Checklist

1. Run against a test account with `health_score = 80`, `tenure_days = 120`, `adoption_checklist_created_at` set → confirm it passes the rule engine and reaches Ollama.
2. Run against an account missing `adoption_checklist_created_at` → confirm it's correctly skipped.
3. Confirm the Odoo activity lands on the right `crm.lead` with the suggestion + rationale text, assigned to `ODOO_SALES_USER_ID`.
4. Force an Ollama malformed-JSON response → confirm it still creates the activity but flags `suggestion_parse_error: true` for manual review, rather than crashing.

---

**Next in Phase 6:** 6.5.1 — Churn win-back sequence. Depends on Module 2.9's "Churn Risk" status tag, already live — unblocked.
