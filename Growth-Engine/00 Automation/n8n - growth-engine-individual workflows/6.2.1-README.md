# 6.2.1 — Adoption Checklist Auto-Create

**Plan ID:** 6.2.1 (Phase 6, session 2)
**Depends on:** "Onboarding" = Module 2.7 (Client Onboarding), already built. Unblocked.
**File:** `6.2.1-Adoption-Checklist-Auto-Create.json`

## Same gap as 6.1.1 — flagged, not invented

This maps to Stage 44 (Customer Success Planning), also confirmed not-yet-pilot-depth in §16.0. Two things in this build are therefore placeholders, clearly marked in the workflow itself:

1. **The 7-day wait before the checklist fires** — no cadence is specified anywhere in the uploads.
2. **The 6 checklist item names** — generic, not service-line-specific. Module 2.7's own README already flags the same limitation for its starter-task template ("fixed regardless of `service_type`"), so this follows the same documented pattern rather than inventing new business content in a different way.

Both need Nivy's review before this is treated as final client-facing process.

## Manual wiring required

Same precedent as 2.6→2.7 (already flagged in §10A.3 of the master plan): add an **Execute Workflow** node at the end of Module 2.7 pointing to this workflow, passing `client_id`, `odoo_project_id`, `odoo_lead_id`, `service_type`. Not done automatically since 2.7 is a live Phase 2 module — this is a decision to apply, not something to silently patch.

## Idempotency

Added `clients_master.adoption_checklist_created_at` and a guard check before creating tasks, so a re-trigger (e.g. workflow retry) doesn't create duplicate checklist tasks — per the supporting context's idempotency rule (§5.4 of that file).

## Test Checklist

1. Trigger manually with a test `odoo_project_id` → confirm 6 tasks appear on that Odoo project.
2. Trigger a second time for the same `client_id` → confirm it short-circuits to `Skip - Already Exists`, no duplicate tasks.
3. Force an Odoo API failure (bad `odoo_project_id`) → confirm it lands in `workflow_errors`, not silently dropped.

---

**Next in Phase 6:** 6.2.2 — Milestone-missed alert (depends on 6.2.1, now unblocked).
